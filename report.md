# 최종 보고서

## 1. 프로그램 주제와 목적

내 프로그램의 주제:55 행렬 기반의 플레이페어 대칭키 암호화 및 복호화 시스템

이 프로그램을 만든 이유: 정보 시간에 대칭키 암호화에 대해서 배운 뒤에 알고리즘으로 구현한다면 재미있을 것 같아서 탐구해보던 중, 우연히 5*5 행렬을 이용하는 플레이페어에 대해서 알게 되었습니다. 이 암호화 방식이 직접 계산하기에는 너무 어려운데 컴퓨터 알고리즘으로 구현하기에는 쉬울 것 같아서 관심이 생겨서 책을 보면서 만들어 보던 중 수행평가 때문에 선정해서 제대로 만들어보게 되었습니다.대칭키 암호화를 통해서 순수 파이썬의 제어 구조와 자료를 활용해서 직접 구현해 봤고 2차원 리스트를 구조화하고 인덱스를 변환하고 제어하며 암호화와 복호화를 실행하며 컴퓨팅 사고력을 늘리기 위해 선정했습니다.


---

## 2. 문제 분해 및 모델링

### 입력
기능 선택 변수 (`choice`): 사용자가 실행할 메뉴 번호 (자료형: 문자열 `str` / 값: `'1'`, `'2'`, `'3'`)
 비밀 대칭키 (`key_str`): 5x5 보안 행렬을 동적으로 구조화하기 위한 나만의 비밀 핵심 단어 (자료형: 문자열 `str` / 예: `'SECURITY'`)
 대상 본문 메시지:
   암호화 시 (`plain_text`): 보호하고자 하는 영문 평문 메시지 (자료형: 문자열 `str` / 예: `'HELLO'`)
   복호화 시 (`cipher_text`): 안전하게 보관 중이던 영문 암호문 메시지 (자료형: 문자열 `str`)

### 처리
 [기능 1] 대칭키 행렬 동적 생성 (`create_matrix`):
   입력받은 `key_str`을 대문자로 변환하고 알파벳 중 중복된 문자를 `for` 반복문과 `if` 조건문으로 검사하여 제거합니다.
   플레이페어 규칙에 따라 'J'를 'I'로 통합하고 남은 알파벳들을 채워 25자의 1차원 리스트를 만든 후, 5글자씩 슬라이싱하여 최종적으로 5x5 크기의 2차원 리스트 `PlayFair`를 모델링합니다.
 [기능 2] 데이터 전처리 및 런타임 예외 통제 (`prepare_text` 및 `main`):
   암호화: 평문의 공백을 제거하고 `while` 루프를 돌며 2글자씩 쌍을 묶을 때, 연속된 동일 문자(예: 'LL')가 발생하면 조건문으로 판정하여 사이에 완충 문자 'X'를 강제 삽입합니다. 최종 글자 수가 홀수일 경우 맨 끝에 'X'를 추가하여 완벽한 짝수 구조를 만듭니다.
   복호화: 암호문을 풀기 전 공백을 제거한 유효 문자열의 길이가 홀수 개인지 `if len(processed_text) % 2 != 0:` 조건문으로 먼저 검증합니다. 홀수일 경우 발생할 수 있는 `IndexError`를 사전에 차단(예외 처리)하고 메뉴로 안전하게 `continue` 시킵니다.
 [기능 3] 행렬 인덱스 제어 기반 알고리즘 연산 (`find_position`, `cipher_pair`):
   전처리가 완료된 본문을 `for` 반복문을 통해 2글자씩 순회하며, 각 문자의 행과 열 위치 좌표를 이중 `for` 루프로 탐색합니다.
   추출된 두 글자의 좌표를 비교하는 복합 조건문(`if-elif-else`)을 통해 '같은 행(가로 순환 축 이동)', '같은 열(세로 순환 축 이동)', '서로 다른 행/열(직사각형 모서리 교차 매핑)'의 3대 규칙을 정밀하게 적용하여 새로운 문자로 치환합니다.

### 출력
 동적 보안 행렬 시각화 데이터: 입력한 대칭키에 의해 생성된 5x5 2차원 배열 구조(`PlayFair`)를 출력하여 암호 체계의 무결성을 사용자에게 시각적으로 보여줍니다.
 데이터 흐름 시각화 리스트 (`pairs_list`): 전처리를 거쳐 2글자씩 결합된 문장 데이터가 내부적으로 어떻게 2차원 리스트 구조로 쪼개져 알고리즘에 입력되었는지 그 과정을 투명하게 출력합니다.
 최종 변환 문자열:
   암호화 기능 선택 시: 일상생활에서 완벽하게 기밀성이 확보된 최종 암호문 문자열을 출력합니다.
   복호화 기능 선택 시: 나만의 대칭키로 안전하게 복구된 최종 평문 본문 내용을 출력합니다.

---

## 3. 사용한 2차원 리스트 설명

2차원 리스트 이름:PlayFair

한 행의 의미:비밀 키(Key) 문자열의 중복을 제거한 글자들과 알파벳 전체 순서에서 남은 글자들을 조합하여 5글자씩 끊어 만든 가로 배열 데이터다. 플레이페어 알고리즘의 핵심 연산 단위이며, 두 글자가 '같은 행'에 속해 있을 때 이 행 내부에서 좌/우 인덱스를 순환 이동시키는 위치 치환 연산의 논리적 기준이다.

각 열의 의미:행렬 내에서 세로축으로 나열된 5개 글자들의 위치를 의미한다. 두 글자의 세로 인덱스가 일치하는지('같은 열 규칙') 판정하는 기준이 되며, '직사각형 모서리 교환 규칙'이 적용될 때 두 글자가 서로의 열 인덱스 좌표를 교차하여 바꾸는 데이터 매핑의 지표당.

| 열 번호 | 의미 | 실제 예시 |
|---:|---|---|
| 0번 열 | 행렬의 1번째 세로축 (각 가로 줄의 0번째 인덱스 문자 모음) | `['S', 'I', 'D', 'L', 'Q']` |
| 1번 열 | 행렬의 2번째 세로축 (각 가로 줄의 1번째 인덱스 문자 모음) | `['E', 'T', 'F', 'M', 'V']` |
| 2번 열 | 행렬의 3번째 세로축 (각 가로 줄의 2번째 인덱스 문자 모음) | `['C', 'Y', 'G', 'N', 'W']` |
| 3번 열 | 행렬의 4번째 세로축 (각 가로 줄의 3번째 인덱스 문자 모음) | `['U', 'A', 'H', 'O', 'X']` |
| 4번 열 | 행렬의 5번째 세로축 (각 가로 줄의 4번째 인덱스 문자 모음) | '['R', 'B', 'K', 'P', 'Z']' |

이 2차원 리스트를 사용한 이유:1. 플레이페어 알고리즘의 수학적 모델링: 플레이페어 암호학의 대원칙은 25개의 알파벳 격자판 안에서 문자들의 기하학적 위치를 비교해 치환하는 것이다. 파이썬의 '2차원 리스트' 자료구조는 행과 열이라는 직관적인 인덱스 좌표 체계를 제공하니까 암호 알고리즘의 격자판 공간을 컴퓨터 과학적으로 모델링하기에 좋아보였다
2. 복합 조건문의 정밀한 흐름 제어: 두 글자 쌍의 위치가 '같은 행'인지, '같은 열'인지, 혹은 '직사각형 모서리 형태'인지 판별할 때, 2차원 리스트의 행/열 인덱스 비교 연산(`r1 == r2` 또는 `c1 == c2`)을 활용하면 복잡한 암호화 알고리즘의 규칙들을 조건문으로 잘 할 수 있을 것 같았다


---

## 4. 함수 설명

| 함수 이름 | 역할 | 입력값 | 반환값 또는 출력 |
|---|---|---|---|
| create_matrix | 입력된 대칭키를 기반으로 중복을 제거하고 알파벳(J 제외)을 채워 5x5 암호 행렬을 생성함 | key_str | 5x5 크기의 2차원 리스트 |
| prepare_text | 평문을 암호화 규격에 맞게 전처리함 (공백 제거, 대문자 변환, J→I, 연속된 동일 문자 및 홀수 글자 수 처리) | plain_text  | processed_text |
| find_position | 5x5 암호 행렬에서 특정 알파벳 문자가 위치한 행과 열 인덱스를 검색함 | char, PlayFair | 문자의 위치를 나타내는 행, 열 번호 |
| cipher_pair | 플레이페어 암호의 3가지 이동 규칙을 적용하여 두 글자 쌍을 암호화하거나 복호화함 | char1, char2, PlayFair, mode | 암호화/복호화가 완료된 두 글자 쌍 |
| process_all_text | 전처리된 전체 텍스트를 2글자씩 슬라이싱하여 cipher_pair 함수를 반복 호출하고 결과를 결합함 | text, PlayFair, mode | 최종 변환된 전체 암호문 또는 복호화문 |
| main | 사용자에게 메뉴(암호화, 복호화, 종료)를 제공하고 입출력을 제어하는 프로그램의 메인 루프를 실행함 | 사용자가 선택한 숫자 | 사용자가 원하는 동작 결과 |

가장 중요한 함수 하나를 골라 자세히 설명하세요.

함수 이름:cipher_pair

설명:cipher_pair 함수는 플레이페어 암호화 알고리즘의 핵심인 '쌍자암호 변환 규칙'을 직접적으로 구현한 가장 중요한 함수다. 이 함수는 전처리된 텍스트에서 두 글자씩 쌍을 이루어 가져온 뒤, 5x5 암호 행렬에서의 상대적 위치에 따라 세 가지 규칙을 시행한다. 첫째, 두 글자가 같은 행에 있으면 mode(encrypt/decrypt)에 따라 shift 변수를 +1 또는 -1로 설정하여 오른쪽이나 왼쪽으로 한 칸씩 이동한다. 둘째, 두 글자가 같은 열에 있으면 위나 아래로 한 칸씩 이동하는데 이때 행렬의 경계를 벗어나지 않도록 나머지 연산자(%)를 활용하여 인덱스를 순환한다. 셋째, 행과 열이 모두 다를 때는 두 글자가 이루는 직사각형의 반대쪽 모서리에 있는 문자로 바꾼다. 이 함수가 중요한 이유는 하나의 함수 내부에서 암호화와 복호화 논리를 shift 변수 하나로 제어하도록 설계되어 코드의 재사용성을 높이고, 플레이페어 암호의 수학적·논리적 규칙이 가장 집약되어 있는 핵심 알고리즘이기 때문이다.


---

## 5. 조건문과 반복문 활용

### 조건문 사용 부분

어떤 조건을 검사했는가?
문자 중복 및 알파벳 유효성 검사 (`create_matrix`): 입력받은 대칭키의 각 글자가 알파벳 기본 배열(`alphabet`)에 포함되는지, 그리고 이미 생성 중인 리스트(`temp_list`)에 중복으로 존재하지 않는지 조건을 검사하여 5x5 행렬의 고유한 값을 구성했다.
텍스트 쌍 구성 조건 검사 (`prepare_text`): 연속으로 나열된 두 글자가 서로 같은지 다른지(`char1 == char2`) 검사하여, 같을 경우 가상 문자 'X'를 삽입하도록 유도했다. 또한 전처리가 끝난 문자열의 전체 길이가 홀수인지(`len % 2 != 0`) 검사하여 마지막에 'X'를 붙여 짝수 쌍을 맞추도록 했다.
행렬 내 위치 관계 규칙 검사 (`cipher_pair`): 두 글자의 행과 열 인덱스를 비교하여 같은 행에 있는지(`r1 == r2`), 같은 열에 있는지(`c1 == c2`),  아니면 서로 다른 행과 열의 직사각형 구조를 이루는지 조건을 나누어 각각 다른 암호화/복호화 알고리즘이 적용되도록 제어했다.
 사용자 입력 유효성 및 메뉴 선택 검사 (`main`): 사용자가 입력한 메뉴 번호가 '1', '2', '3' 중 무엇인지 검사하고, 복호화 기능(2번)에서는 입력된 암호문의 길이가 짝수가 맞는지 검사하여 홀수일 경우 오류 메시지를 출력하고 루프의 처음으로 되돌렸다.


### 반복문 사용 부분

무엇을 반복 처리했는가?
암호 행렬 및 대칭키 글자 순회 (`create_matrix`): 대칭키 문자열(`key_str`)과 알파벳 배열(`alphabet`)을 처음부터 끝까지 한 글자씩 순회하며 중복 없이 25개의 원소를 리스트에 채우는 작업을 반복했다.
 평문 전처리 슬라이싱 (`prepare_text`): `while` 루프를 사용해 인덱스를 조정해가며 평문 전체를 처음부터 끝까지 순회하고, 두 글자씩 확인하며 가상 문자 'X'를 삽입하는 전처리 과정을 반복 처리했다.
 행렬 내 문자 검색 (`find_position`): 5행 5열의 2차원 공간을 중첩 `for` 루프(행 5번, 열 5번)로 총 25번 탐색하며 특정 문자의 위치 인덱스를 찾는 과정을 반복했다.
 전체 텍스트 암·복호화 변환 (`process_all_text`): 전체 문자열을 `range(0, len(text), 2)`를 통해 2글자씩 건너뛰며 순회하고, 각 쌍을 암·복호화 함수에 전달하여 변환된 텍스트를 누적 결합하는 작업을 반복 처리했다.
 메인 메뉴 실행 제어 (`main`): `while True` 무한 루프를 활용해 사용자가 3번(종료)을 선택하여 `break`가 호출되기 전까지 프로그램이 종료되지 않고 계속해서 암호화/복호화 기능을 제공하도록 반복 처리했다.

### 조건문과 반복문이 함께 작동한 부분
 `cipher_pair` 함수 내의 경계 처리 및 `create_matrix` 행렬 생성
 인덱스 순환 구조의 결합: `cipher_pair` 함수에서는 같은 행이나 열에 있을 때 이동 방향에 따라 인덱스가 5를 벗어날 수 있다. 이때 `(c1 + shift) % 5`와 같이 나머지 연산자(%)를 활용한 조건적 계산법을 사용하여, 반복적인 행렬 탐색 알고리즘 내에서 인덱스가 0~4 사이를 자동으로 순환하도록 논리 구조를 결합했다.
 필터링 구조의 결합: `create_matrix`와 `prepare_text` 함수 내에서는 `for` 문과 `while` 문으로 문자열을 반복 순회하는 과정 속에서 `if` 조건문을 배치했다. 이를 통해 중복된 글자는 제외하고 새로운 글자만 리스트에 추가하거나, 동일한 글자가 연속될 때만 조건적으로 'X'를 끼워 넣는 등 데이터의 흐름을 정밀하게 제어하는 '필터링(Filtering) 구조'를 완성해따.

---

## 6. 실행 결과

대표 실행 결과를 붙여넣으세요.

```text
=== 플레이페어 암호 프로그램 ===
1. 암호화
2. 복호화
3. 프로그램 종료
원하는 기능을 선택하세요 (1, 2, 3): 1
비밀 대칭키를 입력하세요: playfair
암호화할 본문을 입력하세요: hide the gold

[!] 생성된 5x5 비밀키 행렬(PlayFair): [['P', 'L', 'A', 'Y', 'F'], ['I', 'R', 'B', 'C', 'D'], ['E', 'G', 'H', 'K', 'M'], ['N', 'O', 'Q', 'S', 'T'], ['U', 'V', 'W', 'X', 'Z']]
[!] 두 글자씩 묶은 2차원 리스트: [['H', 'I'], ['D', 'E'], ['T', 'H'], ['E', 'G'], ['O', 'L'], ['D', 'X']]
[!] 최종 암호문: BMNDZGEVRSYI
```

결과 분석:본 프로그램은 2차원 배열과 인덱스 연산을 활용하여 전통적인 대칭키 암호 방식인 플레이페어(Playfair) 쌍자암호 알고리즘을 성공적으로 구현하였습니다. 프로그램의 동작은 크게 '비밀키 행렬 생성', '평문 전처리', '기하학적 규칙에 기반한 암호화 변환'의 세 단계로 진행됩니다.

먼저, 사용자가 입력한 대칭키의 중복 문자를 제거하고 남은 알파벳을 채워 넣는 방식으로 고유한 5x5 비밀키 행렬을 구성합니다. 이때 행렬의 크기 한계로 인해 알파벳 'J'는 'I'로 통합 처리됩니다. 이후 입력된 본문은 공백이 제거되고 대문자로 일괄 변환되는 전처리 과정을 거칩니다. 플레이페어 암호의 핵심 원리는 문자를 두 글자(한 쌍)씩 묶어서 변환하는 것이므로, 프로그램은 평문을 두 글자씩 슬라이싱하여 2차원 리스트로 구조화합니다. 이 과정에서 글자 수가 홀수로 끝나 일치하는 쌍이 만들어지지 않을 경우, 프로그램이 이를 감지하고 맨 끝에 가상 문자 'X'를 자동으로 삽입하여 알고리즘의 유효성을 확보합니다.

최종적인 암호화 과정은 전처리된 문자 쌍이 5x5 행렬 내에서 가지는 상대적인 위치 관계에 따라 세 가지 일반화된 규칙을 따릅니다. 첫째, 두 글자가 행렬에서 '서로 다른 행과 열'에 위치할 경우, 두 문자는 행렬 상에서 하나의 직사각형을 형성하게 됩니다. 이때 각 문자는 자신이 속한 행을 유지하면서 직사각형의 반대쪽 모서리에 위치한 상대방 열의 문자로 교환됩니다. 둘째, 두 글자가 '동일한 행'에 가로로 나란히 존재할 경우, 각 문자는 오른쪽으로 한 칸씩 이동한 위치의 문자로 변환됩니다. 셋째, 두 글자가 '동일한 열'에 세로로 나란히 존재할 경우, 각 문자는 아래쪽으로 한 칸씩 이동한 위치의 문자로 변환됩니다. 특히 동일한 행과 열에서 이동할 때 행렬의 경계(인덱스 4)를 벗어나는 경우를 대비하여, 코드 내부적으로 나머지 연산자(%)를 활용해 인덱스가 0으로 순환하도록 수학적 예외 처리를 반영했습니다.

결론적으로 이 프로그램은 텍스트 데이터를 2차원 공간의 좌표로 변환하고, 암호학적 기하 규칙을 행렬 인덱스 제어 알고리즘으로 명확하게 처리함으로써 복잡한 쌍자암호 메커니즘을 안정적으로 수행함을 보여줍니다.


---

## 7. 테스트와 오류 수정

### 테스트 1: 정상 사례

입력:1, plat for, dlasd

예상 결과:RFTQBZ

실제 결과:[!] 생성된 5x5 비밀키 행렬(PlayFair): [['P', 'L', 'A', 'T', 'F'], ['O', 'R', 'B', 'C', 'D'], ['E', 'G', 'H', 'I', 'K'], ['M', 'N', 'Q', 'S', 'U'], ['V', 'W', 'X', 'Y', 'Z']]
[!] 두 글자씩 묶은 2차원 리스트: [['D', 'L'], ['A', 'S'], ['D', 'X']]
[!] 최종 암호문: RFTQBZ


### 테스트 2: 예외 또는 경계 사례

입력:2, plat for, R FTQBZ   

예상 결과: DLASDX

실제 결과:[오류] 올바른 암호문 형식이 아닙니다. (유효한 알파벳 글자 수가 홀수입니다.)


### 발견한 오류와 수정한 방법
복호화를 진행할 때 평문에 실수로 공백이나 특수 문자 등 예상하지 못한 문자를 넣으면 프로그램이 그대로 맛이 가 버리는 오류가 생겼다. 이를 수정하기 위해서 제미나이에게 질문을 해서 문자열의 길이가 짝수인지 확인하는 코드를 추가하였다.

---

## 8. Gemini 활용 내용 요약

Gemini에게 도움받은 부분:복호화를 진행할 때 길이가 홀수거나 대문자 이외의 문자가 입력될 때 처리하는 코드가 없었어서 이 부분을 보강해달라고 요청했고 도와준 코드를 추가해서 완성했다


Gemini가 제안했지만 내가 수정하거나 사용하지 않은 부분:제미나이가 이차원 리스트의 가독성을 높이기 위해서 출력 관련 함수를 하나 더 추가하자고 했었지만 코드가 너무 무거워질 것 같았고 어차피 쌤이 보시기에는 알아서 잘 보실 거라고 판단해서 사용하지 않았습니다.


최종 코드에서 내가 직접 판단하거나 수정한 부분:제미나이는 입력창에 설명을 안 써줘가지고 내가 추가적으로 입력창들에 설명들을 추가했다


---

## 9. 한계와 개선 방안

현재 프로그램의 한계:알파벳 J 처리의 불완전성: 플레이페어 암호의 특성상 5x5 행렬을 맞추기 위해 'J'를 'I'로 강제 변환하여 처리했다. 그래서 'JAM', 'JUDGE'와 같은 단어를 복호화했을 때 원문이 'IAM', 'IUDGE'로 복구되어, 문맥을 보고 우리가 때려 맞춰야 하는 한계가 있다.
가상 문자 'X' 처리의 모호함: 연속된 글자(예: HELLO -> HELXLO)나 홀수 글자 수(X 추가)를 맞추기 위해 임의로 넣은 가상 문자 'X'가 복호화 후에도 그대로 남아있어서 원래 본문에 포함되어 있던 진짜 'X'와 암호화를 위해 추가된 가상 문자 'X'를 프로그램이 스스로 구분하여 제거를 못 하고 우리가 또 때려 맞춰야 한다.

다시 개선한다면 추가하고 싶은 기능:확장된 크기의 암호 행렬 도입 (6x6 행렬 등): 5x5 행렬 대신 6x6 행렬로 확장하여 영문 알파벳 26글자 전체와 숫자 0~9까지 동시에 수용할 수 있도록 알고리즘을 확장해서 이를 통해 'J'와 'I'를 통합하지 않고 완벽한 문장 복호화가 가능하게 만들고 싶다.
특수문자 및 공백 보존 기능: 평문에 포함된 공백(띄어쓰기)이나 구두점, 숫자의 위치를 임시 리스트에 저장했다가 암호화가 끝난 후 원래 위치에 다시 삽입하여 문장 구조를 그대로 유지하는 업그레이드를 해 보고 싶다.
복호화 시 가상 문자 'X' 자동 제거 로직 구현: 암호화할 때 'X'가 삽입된 위치나 조건을 기억하거나 복호화 규칙을 정교화해서 원래 단어(예: HELLO)로 깔끔하게 원상 복구해 주는 자동 필터링 기능을 넣어보고 싶다.

---

## 10. 코드인터뷰 준비

다음 질문에 답할 수 있도록 준비하세요.

1. 내 프로그램의 입력-처리-출력은 무엇인가?
2. 2차원 리스트에서 한 행과 각 열은 무엇을 의미하는가?
3. 가장 중요한 함수는 무엇이고 어떤 역할을 하는가?
4. 조건문과 반복문은 어디에서 사용되었는가?
5. Gemini가 도와준 부분과 내가 직접 수정한 부분은 무엇인가?
