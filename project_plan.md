# 프로젝트 계획서

## 1. 프로젝트 주제

내가 만들 프로그램의 주제:

5*5 행렬 기반의 플레이페어 대칭키 암호화 및 복호화 시스템

---

## 2. 문제 상황

이 프로그램이 해결하려는 문제를 설명하세요.

예) 사용자가 현재 기분과 사용할 수 있는 시간을 입력하면, 조건에 맞는 활동을 추천해 주고 싶다.

내 설명: 사용자가 본인이 원하는 대칭키와 평문을 입력하면 그 대칭키로만 풀 수 있는 암호문을 만들어서 일상 생활에서 남들에게 읽히기 꺼려지는 내용들을 안전하게 작성한 뒤 나중에 같은 프로그램을 이용해서 본인이 원할 때 복호화까지 할 수 있는 프로그램을 만들고 싶다. 또한 암호화하는 행렬을 직접 보여줘서 어떤 원리로 암호화와 복호화가 이루어지는지 시각적으로 보여주고 싶다.


---

## 3. 입력-처리-출력 구조

### 입력

사용자가 입력하거나 프로그램이 사용할 데이터는 무엇인가?

- 기능 선택 : 사용자가 암호화를 할 것인지 복호화를 할 것인지 입력받아서 그에 맞는 작업을 수행한다.
- 대칭키 : 5*5 행렬을 구성하는 기준이 될 영어 단어 암호키를 입력받는다.
- 본문 텍스트 : 사용자가 평문을 입력하면 처음에 요청한 암호화 또는 복호화를 실행하는 것에 이용한다.

### 처리

입력된 데이터를 어떤 규칙이나 알고리즘으로 처리할 것인가?

- 입력받은 키워드에서 중복된 글자를 제거한 뒤 I와 J는 같은 글자로 취급하며 키워드 뒤에 남은 알파벳을 붙여서 두 글자씩 이차원 리스트로 변형한다.
- 만약 한 글자가 혼자 남게 된다면 맨 끝에 가상 글자를 추가해서 짝을 맞춘다.
- 입력이 이루어지면 이차원 리스트에서 어떤 행과 열에 있는지 찾은 뒤 같은 행에 있는지, 같은 열에 있는지, 서로 다른 행/열에 있는지 확인한 뒤 각각 오른쪽으로, 아래로, 반대편 모서리 글자로 치환한다.

### 출력

프로그램이 최종적으로 무엇을 출력하는가?

- 사용자가 입력했던 5*5 암호화 행렬과 2차원 리스트를 출력한다
- 암호화 요청이었다면 암호문을, 복호화 요청이었다면 복호화가 완료된 원래의 평문을 출력한다.
---

## 4. 2차원 리스트 설계

내 프로그램에서 사용할 2차원 리스트의 이름:

PlayFair

PlayFair = [
    ["0번 행(Row 0)", ['S', 'E', 'C', 'U', 'R'], "문자열 리스트", "비밀 키 'SECURITY'의 앞부분 글자로 채워진 가로 세트, 암호 연산 시 우측 이동 및 순환의 기준"],
    ["1번 행(Row 1)", ['I', 'T', 'Y', 'A', 'B'], "문자열 리스트", "비밀 키의 뒷부분('I', 'T', 'Y')과 알파벳 시작 글자('A', 'B')가 결합된 가로 세트, 'J'는 'I'로 통합됨"],
    ["2번 행(Row 2)", ['D', 'F', 'G', 'H', 'K'], "문자열 리스트", "중복되지 않은 알파벳 'D'부터 'K'까지 순서대로 배치된 가로 세트, 평문 치환을 위한 인덱스 제공"],
    ["3번 행(Row 3)", ['L', 'M', 'N', 'O', 'P'], "문자열 리스트", "중복되지 않은 알파벳 'L'부터 'P'까지 순서대로 배치된 가로 세트, 연속 문자 분리 후 위치 판정에 사용"],
    ["4번 행(Row 4)", ['Q', 'V', 'W', 'X', 'Z'], "문자열 리스트", "행렬의 마지막을 구성하는 알파벳 가로 세트, 암호화 과정 중 하단 이동 예외 처리 시 상단으로 순환하는 기준"]
]
```

### 한 행의 의미
비밀 키와 나머지 알파벳의 순서대로 저장이 된 5개의 가로 묶음, 글자의 가로 위치 판정 및 오른쪽 치환에 이용 예정

### 각 열의 의미
동일한 세로 축에 위치한 5개의 세로 묶음, 글자의 세로 위치 판정 및 오른쪽을 뺀 나머지 치환에 이용 예정
| 열 번호 | 의미 | 예시 |
|---:|---|---|
| 0번 열 | 각 행의 첫 번째 글자 모음 | ['S', 'I', 'D', 'L', 'Q'] |
| 1번 열 | 각 행의 두 번째 글자 모음 | ['E', 'T', 'F', 'M', 'V'] |
| 2번 열 | 각 행의 세 번째 글자 모음 | ['C', 'Y', 'G', 'N', 'W'] |
| 3번 열 | 각 행의 네 번째 글자 모음 | ['U', 'A', 'H', 'O', 'X'] |
| 4번 열 | 각 행의 다섯 번째 글자 모음 | ['R', 'B', 'K', 'P', 'Z'] |
---

## 5. 함수 설계

| 함수 이름 | 역할 | 입력값 | 반환값 또는 출력 |
|---|---|---|---|
| create_matrix(key_str) | 비밀 키를 기반으로 중복 글자를 제거하고 5*5 이차원 리스트 PlayFair를 생성한다 | key_str | PlayFair |
| prepare_text(plain_text) | 사용자가 입력한 평문 또는 암호문을 2글자씩 묶으며 같은 글자 사이와 혼자 남은 글자에 가상 숫자를 추가한다. | plain_text | processed_text |
| find_position(char, PlayFair) | 특정 알파벳 글자가 2차원 리스트의 몇 행, 몇 열에 위치하는지 찾는다 | char, PlayFair | row, col |
| cipher_pair(char1, char2, PlayFair, mode) | 두 글자 쌍과 행렬을 받아 플레이페어의 3가지 규칙을 조건문으로 판정하여 새로운 두 글자로 치환한다 | char1, char2, PlayFair, mode | new_char1, new_char2
| process_all_text(text, PlayFair, mode) | 전처리가 끝난 전체 문장을 반복문을 통해 2글자씩 잘라 가며 cipher_pair 함수를 반복 호출하고, 변환된 글자들을 합쳐서 최종 결과 문장을 만든다 | text, PlayFair, mode | result_text
| main | 사용자에게 메뉴(암호화, 복호화, 종료)를 제공하고 입출력을 제어하는 프로그램의 메인 루프를 실행함 | 사용자가 선택한 숫자 | 사용자가 원하는 동작 결과 |

---

## 6. 사용할 제어구조

### 조건문을 사용할 부분


내 계획: 암호화, 복호화 중 사용자가 원하는 기능을 실행한다.
이차원 리스트를 만들 때 중복된 글자가 있다면 건너뛴다. 또한 마지막이 혼자면 가상 글자를 추가한다
두 글자의 행과 열이 같은지 검사하여 그에 맞는 치환을 실시한다


### 반복문을 사용할 부분

내 계획:사용자가 특정 버튼을 눌렀을 때만 프로그램이 종료되게 반복한다
비밀키와 알파벳 전체를 비교해가면서 중복되지 않는 것들을 찾는다
25자의 글자를 이차원 리스트 안에 넣을 때 반복하며 넣는다
텍스트를 2글자씩 잘라서 암호화 또는 복호화 함수에 전달하는 것을 반복한다


---

## 7. 예상되는 예외 상황

예) 조건에 맞는 추천 결과가 없을 때, 잘못된 입력을 했을 때 등

1. 비밀키나 평문에 대문자 알파벳이 아닌 숫자 등의 다른 문자가 입력될 때
2. 처음에 원하는 기능을 문자열로 입력할 때 오타가 나서 안내되지 않는 문자열이 입력될 때
3. 복호화를 원하는 암호문이 글자 수가 홀수여서 두 글자씩 묶지 못할 때

---

## 8. Gemini에게 도움받고 싶은 부분

예) 주제 구체화, 2차원 리스트 구조, 함수 분리, 오류 수정 등

1. 주제 구체화
2. 이차원 리스트 순회 코드 개선
3. 디버깅, 예상 못한 예외 상황 발견
4. 전처리한 텍스트와 이차원 리스트 대조 코드 직관화